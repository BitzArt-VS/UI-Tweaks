namespace BitzArt.UI.Tweaks.Gui;

/// <summary>
/// Identifies one side of a rectangle. Used by drawing primitives that operate on a
/// single edge (e.g. open-rect path construction).
/// </summary>
public enum GuiSide
{
    Top,
    Right,
    Bottom,
    Left,
}
