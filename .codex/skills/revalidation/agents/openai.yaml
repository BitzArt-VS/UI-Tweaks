interface:
  display_name: "Agent Revalidation"
  short_description: "Revalidate Codex configuration and project instructions"
  default_prompt: "Use $revalidation to comprehensively revalidate this project's Codex configuration, supporting files, and AGENTS.md instructions against the current codebase, then apply approved updates."
