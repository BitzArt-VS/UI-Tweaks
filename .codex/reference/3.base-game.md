# Vintage Story Reference Locator

This file identifies where to find Vintage Story reference material. It is not a repository of base-game implementation notes by itself.

When a sibling `../BaseGame` is present, follow the instructions in `../BaseGame/AGENTS.md` and use it as optional local reference material. If unavailable, rely on `resources/lib/` and public references.

While working with files from `../BaseGame`, validate its reference material against actual implementation details you encounter and propose updates to that project using the active workflow's required format when one applies, otherwise using the normal `Findings` format. This includes important implementation details, curious quirks, relevant examples, and anything else that may help future work.

Maintain the actual reference material proactively as new information is discovered. Update this locator only when the locations or workflow described here change.
